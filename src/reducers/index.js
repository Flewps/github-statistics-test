
// import { combineReducers } from 'redux'

let initialState = {
  data: {}
}

function mainReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export const reducer = mainReducer // combineReducers(mainReducer)